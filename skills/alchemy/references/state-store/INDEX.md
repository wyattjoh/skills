# state-store index

2 pages. How Alchemy persists resource state between deploys to compute diffs and track infrastructure.

| Page | File | Covers |
| --- | --- | --- |
| State Store | `_overview.md` | How Alchemy persists resource state between deploys to compute diffs and track infrastructure. |
| Custom State Store | `custom-state-store.md` | Build a Postgres-backed Alchemy state store step by step — implement the StateService interface, plug it into a stack, and test it end-to-end. |
