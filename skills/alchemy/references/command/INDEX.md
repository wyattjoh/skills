# command index

3 pages. Alchemy's cloud-agnostic primitives for local processes in the deploy graph — memoized builds, one-off commands, and dev servers.

| Page | File | Covers |
| --- | --- | --- |
| Command | `_overview.md` | Alchemy's cloud-agnostic primitives for local processes in the deploy graph — memoized builds, one-off commands, and dev servers. |
| Dev servers | `dev-servers.md` | Run a framework dev server as a Command.Dev resource — started by alchemy dev, a no-op on alchemy deploy, with the server's URL surfaced back into your stack. |
| Memoization: builds & one-off commands | `memoization.md` | Content-hash memoization shared by Command.Build and Command.Exec — what gets hashed, when a command re-runs, and the outdir contract on destroy. |
