# docker index

5 pages. Images, containers, networks, and volumes as Stack resources, driven through your active Docker CLI context.

| Page | File | Covers |
| --- | --- | --- |
| Docker | `_overview.md` | Images, containers, networks, and volumes as Stack resources, driven through your active Docker CLI context. |
| Build & push images | `build-and-push.md` | Build a container image from a Dockerfile, push it to a registry, and hand the resulting image reference to Cloudflare Containers or AWS ECS. |
| Local services: Postgres with containers, networks, and volumes | `local-services.md` | Run a local Postgres for development as alchemy resources — a pinned image, a named network and volume, a Redacted password, and a healthchecked container that converges on every deploy. |
| Setup | `setup.md` | Point alchemy at a Docker daemon — the active CLI context and the DOCKER_BIN override. |
| Docker Swarm | `swarm.md` | Provision a swarm and deploy replicated services to it, from local dev to a remote VPS |
