# github index

5 pages. Repositories, Actions secrets and variables, webhooks, and repository event sources as Stack resources — the glue for CI/CD.

| Page | File | Covers |
| --- | --- | --- |
| GitHub | `_overview.md` | Repositories, Actions secrets and variables, webhooks, and repository event sources as Stack resources — the glue for CI/CD. |
| Actions secrets & variables | `actions-config.md` | Manage GitHub Actions secrets and variables as Stack resources — encrypted secrets, plain-text variables, bulk helpers, and CI credentials provisioned as code. |
| Webhooks & events | `events.md` | Provision GitHub repository webhooks as resources, or subscribe a Cloudflare Worker to repository events with typed payloads and verified deliveries. |
| Repositories | `repository.md` | Manage GitHub repositories as Stack resources — create or adopt a repo, converge its settings on every deploy, rename in place, and opt into deletion explicitly. |
| Setup | `setup.md` | Connect alchemy to GitHub — account, credentials, and profiles. |
